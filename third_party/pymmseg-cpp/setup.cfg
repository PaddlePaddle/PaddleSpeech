# [options]
# python_requires = >=3.7
# setup_requires =
#     setuptools
#     wheel
#     pybind11
# packages = find:

# [build-system]
# requires = ["setuptools>=42", "wheel", "pybind11~=2.6.1"]
# build-backend = "setuptools.build_meta"